{
  "name": "mdshare-server",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js",
    "test": "node --test",
    "test:coverage": "c8 --include app.js --include cleanup.js --include env.js --check-coverage --lines 85 --branches 75 --functions 80 node --test"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "c8": "^12.0.0",
    "supertest": "^7.2.2"
  }
}
